# Source Code

This directory holds the durable pipeline foundation for the project.

Rules:

- keep workflow-aligned package boundaries
- avoid mixing prototype UI code into this tree
- prefer explicit contracts and typed models over informal dict-shaped data flows

The initial package is `semisupply`.
