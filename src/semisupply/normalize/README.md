# Normalize

This area holds transformations from source-bound records into normalized entities and bridge layers.

Examples:

- source row to canonical identifier mapping
- company resolution and source crosswalks
- facility operator matching
- normalized values and bridge-code mapping
