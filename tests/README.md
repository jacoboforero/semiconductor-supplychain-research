# Tests

This directory holds tests and small representative fixtures.

Structure:

- `unit/` for isolated logic
- `integration/` for workflow slices
- `fixtures/` for small versioned sample data only
