# Scripts

This directory should hold thin operational entry points only.

Examples:

- local ingestion runs
- snapshot capture commands
- graph load commands

Business logic should live in `src/semisupply/`, not directly in scripts.
